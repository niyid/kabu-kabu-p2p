<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest — kabu-kabu-p2p

    Privacy design:
      - No WRITE_EXTERNAL_STORAGE: trip data stays in app-private Room DB
      - Location is collected only to compute a GeoHash-5 cell (~5 km)
        and is NEVER transmitted in raw form over any network
      - All peer communication goes through the embedded I2P router;
        no cleartext traffic is permitted outside of I2P reseed bootstrapping
        (handled in network_security_config.xml)
      - Battery optimisation exemption requested so the I2P router stays
        alive and drivers/couriers remain reachable in the background
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techducat.kabukabup2p"
    android:versionCode="1"
    android:versionName="0.1.0-p2p" >

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="36" />

    <!-- Network (I2P + reseed bootstrapping) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required for I2P UDP multicast peer discovery on LAN -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Location — GeoHash cell computation only; raw GPS never leaves device -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Background location: drivers need to stay discoverable while backgrounded -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Audio -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Keep I2P router alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Camera (optional proof-of-delivery photo — stored locally) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <permission
        android:name="com.techducat.kabukabup2p.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.techducat.kabukabup2p.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true" />

    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <application
        android:name="com.techducat.kabukabup2p.KabuKabuApp"
        android:allowBackup="false"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KabuKabu" >

        <!--
            MainActivity — rider / sender UI
            Handles ride/courier requests, live peer-map (GeoHash cells only),
            trip history (local Room DB only)
        -->
        <activity
            android:name="com.techducat.kabukabup2p.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KabuKabu" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            I2PKabuService — replaces any central WebSocket relay.
            Runs the embedded I2P router and the local Matchmaking Service
            (127.0.0.1:8881) as a foreground service.
            Drivers keep this running in the background so they remain
            discoverable by riders/senders in their GeoHash cell.
        -->
        <service
            android:name="com.techducat.kabukabup2p.service.I2PKabuService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync|remoteMessaging|location" />

        <!-- DriverActivity — driver/courier runner UI -->
        <activity
            android:name="com.techducat.kabukabup2p.DriverActivity"
            android:exported="false"
            android:theme="@style/Theme.KabuKabu" />

        <!-- TripHistoryActivity — on-device trip history (no server fetch) -->
        <activity
            android:name="com.techducat.kabukabup2p.TripHistoryActivity"
            android:exported="false"
            android:parentActivityName="com.techducat.kabukabup2p.MainActivity"
            android:theme="@style/Theme.KabuKabu" />
        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="androidx.window.extensions"
            android:required="false" />
        <uses-library
            android:name="androidx.window.sidecar"
            android:required="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.techducat.kabukabup2p.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <service
            android:name="androidx.room.MultiInstanceInvalidationService"
            android:directBootAware="true"
            android:exported="false" />

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.bugfender.sdk.ui.FeedbackActivity"
            android:exported="false"
            android:theme="@style/NoActionBar"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>